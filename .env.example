# Copy to .env.local and fill in. Never commit real values.
OPENAI_API_KEY=
LANGSMITH_API_KEY=
LANGSMITH_ENDPOINT=https://eu.api.smith.langchain.com
# The app sets this itself (mvp/runtime.py); named here so the value is discoverable.
LANGSMITH_PROJECT=capstone-mvp
